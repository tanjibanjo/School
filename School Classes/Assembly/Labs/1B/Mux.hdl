// Lane Pollock
// 3 Sep 2025
// Lab 01B
/** 
 * Multiplexor:
 * if (sel = 0) out = a, else out = b
 */
CHIP Mux {
    IN a, b, sel;
    OUT out;

    PARTS:
    //sel
    Not(in=sel, out=nsel);
    And(a=a, b=nsel, out=c);
    And(a=b, b=sel, out=d);
    Or(a=c, b=d, out=out);
}